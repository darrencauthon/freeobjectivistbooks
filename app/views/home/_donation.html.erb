<div class="grid_6 request">
    <p class="headline"><span class="title"><%= donation.book %></span> to:</p>
    <p class="name"><%= donation.user.name %></p>
    <p class="address">
        <% if donation.user.address.present? %>
        <%= format_address donation.user.address %>
        <% else %>
        This student hasn't given their full address yet. We'll contact them to fill it in and then notify you.
        <% end %>
    </p>
</div>
<div class="grid_4 request actions">
    <p><%= link_to 'See full request details', request_path(donation) %></p>
    <% if donation.flagged? %>
    <p class="flagged">Shipping info flagged</p>
    <% elsif donation.user.address.present? %>
    <p><%= link_to 'Flag problem with shipping info', flag_request_path(donation) %></p>
    <% end %>
</div>
<div class="clear"></div>
